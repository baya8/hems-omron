# プロジェクト文脈: OMRON 太陽光・分電盤計測システム連携

このファイルは、AIエージェントが本プロジェクトの特殊な仕様を即座に把握するためのコンテキスト指示書です。

## 1. プロジェクトの目的
オムロン製計測ユニット（MCSM-P03）から、ECHONET Lite および HTTP API を使用して、リアルタイムの電力データと過去の履歴データを取得・活用する。

## 2. 確定した技術仕様（重要）

### デバイス情報
- **IPアドレス:** `192.168.50.25`
- **メーカー:** オムロン (メーカーコード: `0000D5`)
- **機器構成:** 太陽光発電 (`0x0279`) ×2、分電盤計測 (`0x0287`) ×1

### リアルタイム取得 (ECHONET Lite)
標準的な `0xE0` 等が SNA (0x52) を返すため、以下の独自マッピングを使用する。
- **発電電力:** `0x0279-01` の `0xE0` (2byte, W)
- **主幹電力:** `0x0287-01` の `0xC6` (4byteのうち後半2byte, W) ※正=買電 / 負=売電
- **電圧:** `0x0287-01` の `0xC8` (4byte, 0.1V単位)
- **消費電力:** `発電電力 + 主幹電力` で算出

### 履歴取得 (HTTP API / CGI)
ECHONET Liteの履歴プロパティは書き込み禁止のため、Webサーバー経由で取得する。
- **エンドポイント:** `/getinfo.cgi`
- **デバイスID:** `00168978`
- **パラメータ:** `IG0` (発電), `ISI` (売電), `IBI` (買電)
- **算出:** `消費量 = IG0 + IBI - ISI`

## 3. トラブルシューティング済みの事項
- **WSL2通信:** Windowsファイアウォールで UDP 3610 ポートの受信許可が必須。
- **送信元ポート:** リクエスト送信元を `3610` に固定することでパケット詰まりを回避。
- **データ形式:** CGIのレスポンスは非標準なHTTPヘッダーを含むため、Goの `net/http` ではなく生の TCP (`net.Dial`) で取得する必要がある。

## 4. 現在のメインツール
- `omron_energy_tool.go`: リアルタイム監視 (`monitor`) と CSV保存 (`export`) を統合したツール。

## 5. 次回以降のタスク候補
- 取得したデータのグラフ化（Webダッシュボード化）。
- 毎日決まった時間に CSV を自動更新する常駐処理の実装。
- 30分ごとのリアルタイム値を記録し、欠落のない詳細ログを作成する機能。
